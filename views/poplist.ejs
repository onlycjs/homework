<!DOCTYPE html>
<html lang="en">
<% include layout/head %>

<body>
    <% include layout/nav %>
    <div class="container">
        <div class="row mt-4">
            <div class="col-10 offset-1">
                <h1><%= msg %></h1>
                <ul class="list-group">
                    <% for(let i = 0; i < list.length; i++) { %>
                    <li class="list-group-item">
                        <%= list[i] %> <a href="/popinfo" class="btn btn-info" style="position:absolute; right:10px; bottom:5px; color: #fff;">정보</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>
</body>

</html>